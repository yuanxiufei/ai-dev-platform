[project]
name = "task-queue"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "celery[redis]>=5.4.0",
    "python-dotenv>=1.0.0",
]

[tool.hatch.build.targets.wheel]
packages = ["."]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
