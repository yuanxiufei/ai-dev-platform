[project]
name = "workers"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "celery[redis]>=5.4.0",
    "ai-models",
    "queue",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
