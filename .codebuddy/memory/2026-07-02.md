# 2026-07-02

## Session 25: VSCode / hermes 功能对标继续

### 已完成
1. **OutlinePanel.vue (~300行)** — 文档大纲/符号树
   - 多语言 Regex 解析器（TS/JS/Python/Go/Rust/Vue/CSS/JSON）
   - 基于缩进的树形层级构建（class/function 为父节点）
   - 递归 OutlineNode 组件渲染，VSCode 风格缩进+图标+行号
   - 光标位置追踪，自动高亮当前所在符号
   - 集成到 Sidebar.vue Explorer 视图第三区域"大纲"

2. **WelcomePage.vue (~160行)** — VSCode 起始页
   - Logo+标语 Hero 区域
   - 6 个快捷操作（新建文件/打开文件夹/克隆仓库/新建项目/AI对话/终端）
   - 带快捷键提示的 hover 交互卡片
   - 底部帮助链接栏
   - 集成到 EditorArea: `store.tabs.length === 0` 时显示

3. **useMarkdown.ts (~350行)** — 自研 Markdown 渲染引擎
   - 解析器: 标题/段落/代码块/列表/引用/表格/分割线/HTML注释
   - 特殊块: Mermaid 图表块 + KaTeX 数学公式块
   - 内联渲染: **粗体**/*斜体*/~~删除~~/`代码`/链接/图片/自动链接/行内KaTeX
   - 代码高亮: 关键词着色+字符串+注释+数字
   - CDN 动态加载 Mermaid/KaTeX（零 npm 依赖）
   - 代码块复制按钮
   - postProcessMarkdown() 统一后处理

4. **ChatPanel Markdown 集成**
   - import renderMessageHtml + v-html 替换 {{ content }}
   - onUpdated 钩子延迟 50ms 处理 Mermaid/KaTeX 渲染
   - 完整 Markdown CSS 样式集（h1-h6/p/strong/em/code/blockquote/table/mermaid/katex）
   - AI 和用户消息气泡都支持 Markdown

5. **Monaco 编辑器增强**
   - `renderLineHighlight: "all"` → VSCode 整行高亮
   - `guides.highlightActiveIndentation: true` → 活动缩进高亮
   - `rulers: [80, 120]` → 80/120 字符标尺线
   - `renderWhitespace: "selection"` → 选区显示空白字符
   - `overviewRulerLanes: 3` → 3 条概览标尺
   - `showFoldingControls: "mouseover"` → 悬停折叠控件
   - `lineNumbersMinChars: 4` → 行号最小宽度
   - 右键菜单新增: 显示空白字符, 切换行高亮
   - 主题颜色新增: editorLineHighlightBorder, editorRuler.foreground

### 修改文件
| 文件 | 操作 | 行数 |
|------|------|------|
| OutlinePanel.vue | 新建 | ~300 |
| WelcomePage.vue | 新建 | ~160 |
| useMarkdown.ts | 新建 | ~350 |
| Sidebar.vue | 修改 (+OutlinePanel) | +20 |
| EditorArea.vue | 修改 (+WelcomePage) | +3 |
| ChatPanel.vue | 修改 (Markdown集成) | +120 |
| CodeEditor.vue | 修改 (Monaco增强) | +15 |
| MEMORY.md | 更新 Session 25 | +1 |

### 对标分析
- **VSCode**: Outline Panel, Welcome Page, 编辑器增强(标尺/行高/缩进), 右键菜单增强
- **hermes-studio**: Mermaid/KaTeX Markdown 渲染 (参考其 MarkdownRenderer.vue)

### 状态
全部 0 lint 错误。

---

## Session 26: PeekView + QuickDiff + ChatOutline + GlobalSearch增强

### 已完成
1. **PeekView.vue (~140行)** — VSCode Peek 悬浮预览
   - 悬浮窗口内嵌 Monaco Editor（只读），查看定义/引用
   - 支持多个结果：next/prev 导航箭头
   - open-full 按钮（在完整编辑器打开）
   - 异步加载 Monaco，revealLineInCenter 定位目标行
   - 集成到 EditorArea: `__openPeek` / `__closePeek` 挂载 window 供跨组件访问

2. **useQuickDiff.ts (~110行)** — 行内差异装饰 (Dirty Diff)
   - `computeLineDiff(original, current)`: LCS 启发式行级差异算法
   - `diffLinesToDecorations(diffLines)`: 转 Monaco `IModelDeltaDecoration[]`
   - 颜色: 修改(glyph+line)=#d29922, 新增=#2ea043, 删除=#f85149
   - CodeEditor.vue 集成: `onDidChangeModelContent` 延迟 300ms → applyQuickDiff()
   - Monaco glyphMargin + overviewRuler + className 三通道装饰
   - CSS: `.qd-glyph-*`, `.qd-*-line` 全局样式

3. **ChatOutlinePanel.vue (~150行)** — 对话大纲导航
   - 从 AI 回复中提取 H1-H3 标题（层级可视化）
   - Per-message 计数器生成 ID: `chat-h-{msgIdx}-{hCount}`
   - 平铺渲染 + 视觉缩进 (getVisualDepth)
   - 折叠/展开 (collapsed Set)
   - 点击跳转 + 高亮动画 (.outline-highlight)
   - ChatPanel 集成: ListTree 图标，与 toolPanel 互斥切换

4. **GlobalSearch.vue 增强** — VSCode 文件分组树形搜索
   - FileGroup 接口: file, results, collapsed
   - groupedResults computed: 按文件分组
   - 文件头行: ChevronRight/Down 折叠图标 + FileText + 文件名 + 路径 + 匹配计数 badge
   - 可折叠结果列表: Hash 图标 + 行号 + 高亮匹配文本
   - flatIndexOf 跨组索引匹配 currentResultIndex 高亮

5. **useMarkdown.ts 增强**
   - renderMarkdownToHtml 新增 headingPrefix 参数
   - 标题块生成 `{headingPrefix}-{hCount}` 格式 ID
   - ChatPanel renderMessageHtml 传递 `chat-h-{msgIdx}` 前缀

### 修改文件
| 文件 | 操作 | 行数 |
|------|------|------|
| PeekView.vue | 新建 | ~140 |
| useQuickDiff.ts | 新建 | ~110 |
| ChatOutlinePanel.vue | 新建 | ~150 |
| EditorArea.vue | 修改 (+PeekView集成) | +40 |
| CodeEditor.vue | 修改 (+QuickDiff+PeekView右键) | +35 |
| ChatPanel.vue | 修改 (+ChatOutline集成) | +50 |
| useMarkdown.ts | 修改 (+headingPrefix) | +20 |
| GlobalSearch.vue | 修改 (文件分组) | +50 |

### 对标分析
- **VSCode**: Peek Definition, QuickDiff/Dirty Diff, Outline View, 文件分组搜索结果
- **hermes-studio**: 对话大纲 (参考其 ChatSidebar navigation)

### 状态
全部 8 文件 0 lint 错误。
