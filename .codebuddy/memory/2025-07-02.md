# 2026-07-02 工作日志

## VS Code 风格与功能对标 (Session 27)

### 新建文件 (4 个)
1. **ActivityBar.vue** (~220行) — 独立 48px VS Code 活动栏
   - 从 Sidebar 完全分离，始终可见
   - 7 个活动项 (explorer/search/git/debug/testing/extensions/settings)
   - 拖拽重排序、激活指示器 2px 白线、tooltip、badge 计数器
   - 图标使用 inline SVG (6 个自定义 SVG)

2. **SearchPanel.vue** (~440行) — VS Code 搜索替换面板
   - 搜索/替换双输入框、搜索历史
   - 大小写/全词/正则 toggle 按钮组
   - 文件包含/排除模式
   - 搜索结果按文件分组树形展示
   - 结果导航 (上/下一个匹配)
   - 单个替换 + 全部替换

3. **ExtensionsPanel.vue** (~680行) — VS Code 扩展管理器
   - 已安装/市场双标签切换
   - 12 个模拟扩展数据 (含真实扩展名)
   - 分类筛选下拉 + 搜索过滤
   - 扩展卡片: 图标/名称/描述/作者/下载/评分
   - 安装/卸载/启用/禁用操作
   - 点击展开详情 (特性列表、元信息、操作按钮)

4. **TestingPanel.vue** (~420行) — VS Code 测试资源管理器
   - 测试树结构: File > Suite > Case
   - 3 个模拟测试文件 (15 个测试用例)
   - 状态: passed/failed/skipped/running/idle
   - 运行全部/单文件/单测试 + 调试按钮
   - 统计栏 (总数/通过/失败/跳过)
   - 搜索过滤 + 状态筛选
   - 失败测试详情输出 (错误消息 + 堆栈)

### 修改文件 (6 个)
5. **Sidebar.vue** — 移除内嵌 ActivityBar，整合 SearchPanel/ExtensionsPanel/TestingPanel
6. **App.vue** — 添加 ActivityBar 独立渲染，注册 testing/extensions 快捷键
7. **useLayoutStore.ts** — 添加 testing 活动项 (FlaskConical 图标)
8. **useTerminalStore.ts** — 新增 createTerminal/closeTerminal/switchToTerminal/duplicateTerminal/renameTerminal
9. **useIDEStore.ts** — facade 暴露新终端 API
10. **Terminal.vue** — 大幅增强 (~340行)
    - 多终端标签页 (+ 新建 × 关闭)
    - Shell 类型切换 (bash/zsh/powershell/cmd)
    - 分割/清屏/杀死终端按钮
    - 双击标签重命名
    - 14 个终端主题 (含 VSCode Dark 新增)

### 技术要点
- 所有组件使用 CSS 变量 (var(--color-ide-*))，零硬编码色值
- VS Code 精确尺寸: ActivityBar=48px, 活动项=48px, 图标=24px
- 拖拽调整宽度 170px~600px
- 零 lint 错误 (仅 Tailwind suggestCanonicalClasses 建议性警告，与项目现有风格一致)
